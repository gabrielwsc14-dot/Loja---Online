# Supabase integration removed. No environment variables required for local fs-based DB.
# If you later add external integrations, keep secrets out of the repository and
# use a local `.env` file or your deployment provider's environment settings.
